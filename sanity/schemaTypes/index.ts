import post from './post';
import job from './job';
import event from './event';
import jobApplication from './jobApplication';
import page from './page';

export const schema = {
  types: [post, job, event, jobApplication, page],
};
